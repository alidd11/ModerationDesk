# Command inventory

## Moderation

`/mod warn`, `/mod warnings`, `/mod clear-warning`, `/mod kick`, `/mod ban`, `/mod tempban`, `/mod softban`, `/mod unban`, `/mod timeout`, `/mod untimeout`, `/mod nickname`, `/mod note`, `/mod notes`

## Cases

`/case view`, `/case history`, `/case reason`, `/case void`

## Channels

`/channel purge`, `/channel lock`, `/channel unlock`, `/channel slowmode`, `/channel nuke`

## Security

`/security status`, `/security automod`, `/security automod-action`, `/security blocked-word-add`, `/security blocked-word-remove`, `/security blocked-word-list`, `/security allow-domain-add`, `/security allow-domain-remove`, `/security anti-raid`, `/security anti-nuke`, `/security trust-user`, `/security trust-role`, `/security exempt-role`, `/security exempt-channel`, `/security lockdown`, `/security unlockdown`

## Roles

`/roles add`, `/roles remove`, `/roles autorole-add`, `/roles autorole-remove`, `/roles sticky-toggle`, `/roles sticky-role-add`, `/roles sticky-role-remove`, `/roles list`

## Community

`/community announce`, `/community poll`, `/community giveaway`, `/community giveaway-reroll`, `/community embed`, `/community starboard`, `/suggest`

## Utilities

`/utility ping`, `/utility serverinfo`, `/utility userinfo`, `/utility avatar`, `/utility roleinfo`, `/utility afk`, `/utility diagnose`, `/utility invite`, `/utility dashboard`, `/utility privacy`

## Verification and migration

`/verify setup`, `/verify disable`, `/verify refresh`, `/verify status`

`/migration setup`, `/migration role-map`, `/migration role-unmap`, `/migration start`, `/migration stop`, `/migration status`

## Configuration and appeals

`/config logs`, `/config welcome`, `/config goodbye`, `/config suggestions`, `/config appeals`, `/config staff-role`, `/config admin-role`, `/config show`, `/config export`

`/appeal list`, `/appeal view`, `/appeal resolve`

## Plans

`/premium status`, `/premium features`, `/premium subscribe`
